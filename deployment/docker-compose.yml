version: "3.9"

services:
  ml_api:
    build:
      context: ..
      dockerfile: deployment/Dockerfile

    container_name: evoastra_team_E_api

    ports:
      - "8000:8000"

    volumes:
      - ../models:/app/models
      - ../data:/app/data

    restart: always